<!DOCTYPE html>
<html>
  <head>
    <title>ArticlePage</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;900&display=swap" rel="stylesheet">
  </head>
  <body class="bkg-tm">
    <div>
      <nav class="navbar navbar-expand-lg navbar-dark bg-white mt-2 margin-side-nav">
        <div class="container-fluid">
          <div class="collapse navbar-collapse d-flex justify-content-between align-items-center" id="navbarColor01">
            <%= image_tag "logo.png", :class => "logo" %>
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <%= link_to 'Home', root_path, :class => "nav-link fw-bold th-or" %>
              </li>
              <li class="nav-item">
                <%= link_to 'Create Project', new_project_path, :class => "nav-link text-muted"%>
              </li>
              <li class="nav-item">
                <%= link_to 'Categories', categories_path, :class => "nav-link text-muted"%>
              </li>
              <li class="nav-item">
                <%= my_projects %>
              </li>
            </ul>
            <form class="d-flex margin-b">
              <% if logged_in? %>
                <%= link_to 'logout', logout_url, method: :delete%>
                <p class= "mx-2">|</p>
                <%= current_user.username %>
              <% else %>
                <%= link_to 'login', login_url%>
                <p class= "mx-2 mb-0">|</p>
                <%= link_to 'register', register_url%>
              <% end %>
              <input class="form-control me-2 ml-2" type="search" placeholder="Search" aria-label="Search">
              <a class="s-btn"><%= fa_icon "search" %></a>
            </form>
          </div>
        </div>
      </nav>
      <div class="nm bg-white">
        <% flash.each do |name, msg| %>
          <div class="container bg-notice p-2"><%= msg %></div>
        <% end %>
      </div>
    </div>
    <%= yield %>
    <footer class="text-center text-lg-start">
      <div class="d-flex justify-content-between align-items-center">
        <%= image_tag "logo.png", :class => "logo" %>
        <div class="text-center text-white p-3 bg-th">
          <a class="th-or fw-bold" href="#">Privacy Policy  Terms & Conditions</a>
          © 2021 Copyright: All Rights Reserved
        </div>
        <div class="d-flex mr-4 align-items-center">
          <p class="text-white mb-0 mr-2 fs">Contact with us: </p>
          <p class="s-btn s-icon"><%= fa_icon "facebook" %></p>
          <p class="s-btn s-icon"><%= fa_icon "twitter" %></p>
          <p class="s-btn s-icon"><%= fa_icon "instagram" %></p>
        </div>
      </div>
    </footer>
  </body>
</html>
