<div class="col-6">
  <div class="d-flex justify-content-between">
    <p><%= obj.title %></p>
    <%= link_to 'Edit', edit_project_path(obj) %>
  </div>
  <%= my_projects_image(obj) %>
  <p><%= reduce_text(obj.text) %>
</div>